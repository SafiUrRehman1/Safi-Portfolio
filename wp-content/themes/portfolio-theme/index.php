<?php
/**
 * Fallback template required by WordPress for a valid theme.
 * Real templates (front-page.php, single-project.php, archive-project.php) are added during implementation.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

get_header();
get_footer();
